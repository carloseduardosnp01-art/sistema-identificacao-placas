# Pasta de Modelos

Coloque o arquivo de pesos treinado (`best.pt`) obtido do Google Colab nesta pasta:
- Caminho: `models/best.pt`

Caso o arquivo `best.pt` não esteja presente, o sistema usará automaticamente o modelo `yolov8n.pt` como fallback.
